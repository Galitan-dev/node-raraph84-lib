const {
    http: {
        Endpoint,
        HttpServer,
        Request,
    },
    time: {
        Duration,
    }
} = require('./src');

module.exports = {
    Endpoint,
    HttpServer,
    Request,
    Duration
};